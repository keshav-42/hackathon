module.exports = {
    plugins: [
      require('@tailwindcss/postcss'), // Use the new PostCSS plugin
      require('autoprefixer'), // Optional: Adds vendor prefixes for better browser compatibility
    ],
  };